{
  "name": "json-server",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "npm run build",
    "start": "node server.js"
  },
  "author": "Léo Serrão",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.17.1"
  }
}
